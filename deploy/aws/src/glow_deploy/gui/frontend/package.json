{
  "name": "glow-deploy-gui-frontend",
  "private": true,
  "type": "module",
  "description": "Dev-time TypeScript sources compiled to gui/static/js/ — never shipped to end users.",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "happy-dom": "^20.11.2",
    "typescript": "^5.6.0"
  }
}
